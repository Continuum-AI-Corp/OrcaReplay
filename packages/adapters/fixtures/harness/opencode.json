{
  "adapter": "opencode",
  "harness_versions": null,
  "verified_at": "2026-09-07",
  "command": "opencode",
  "env_vars": [
    "ANTHROPIC_API_KEY",
    "ANTHROPIC_BASE_URL",
    "OPENAI_API_KEY",
    "OPENAI_BASE_URL",
    "OPENCODE_CONFIG_CONTENT",
    "SHELL"
  ],
  "base_urls": {
    "OPENAI_BASE_URL": "/v1",
    "ANTHROPIC_BASE_URL": "/v1"
  },
  "optional_env_vars": [],
  "note": "Verified with OpenCode 1.18.29 using isolated fake OAuth/API-key credentials and a local upstream stub. ChatGPT OAuth rewrites the URL after OPENAI_BASE_URL is applied; a run-local plugin captures the final fetch to chatgpt.com/backend-api/codex/responses or the public OpenAI model endpoints. OPENCODE_CONFIG_CONTENT retains existing settings and plugins while appending the capture plugin. The first-party provider overlay and SHELL shim routing remain in place. The fixture keys describe the no-auth placeholder branch. See docs/opencode-capture.md for reproduction and transport limits. The Anthropic base includes /v1 because OpenCode appends /messages, unlike Claude Code. Verified through the built orca record CLI with an Anthropic API-key fixture."
}
