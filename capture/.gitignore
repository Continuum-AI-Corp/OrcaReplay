# Captures stay local. The request bodies carry session and message ids, the raw orca runs carry
# account and device ids, and none of it is needed to use the tool. Only the script, the
# documents and the scrubbed prompts under ../prompt/ are committed.
*/
index.json

# The Chinese runbook is kept locally. CAPTURE-RUNBOOK.md is the committed one.
*.zh-CN.md
